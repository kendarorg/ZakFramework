* Add the "Is folder" Plugin (allowed only with tree plugin)


* User management admin
	1) users
	2) groups
	3) users+groups
	4) groups inheritance->synchronization of permissions

* Applied user management admin
	1) TablePermission CRUD
	2) functional permissions (forms)
	3) filter permissions (only english and northwind )

* User management "users"
	1) filter permissions (only english and northwind )
	2) functional permissions (contents/tags)

* Setup "Seo firendly" recursive Path
* Build where conditions without writing strings (recycle zqueue)
* Multidatabase support (recycle zqueue)
