@model IEnumerable<ZakCms.Models.Entitites.UserModel>
<table>
	<tr>
		<th>
			Code
		</th>
		<th>
			Description
		</th>
	</tr>
	@foreach (var item in Model)
	{
		<tr>
			<td>
				@Html.ActionLink(item.UserId, "Index", new {id = item.Id, view = "Details"})
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.UserId)
			</td>
		</tr>
	}
</table>